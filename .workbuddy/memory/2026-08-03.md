# 2026-08-03

## WSL 轮询间隔限制收紧
- 需求：默认 10s（已是 10000ms，未变）；将合法范围上界从 10 分钟改为 1 分钟。
- 改动文件：
  - `electron-main.js`（真实服务端，主进程直接运行无需打包）：
    - 启动读取校验 `wslPollInterval <= 600000` → `<= 60000`（约 320 行）。
    - `PUT /api/settings` 校验与错误文案 `500–60000`（约 596/600/601 行）。
    - 注释「合法范围 [500, 60000]」。
  - `client/src/App.jsx`（前端校验 + UI）：
    - `saveSettings` 校验与错误文案 `val > 60000`（约 334/335 行）。
    - 设置弹窗 `<input max="60000">` 及 hint「Range 500–60000 ms」（约 876/882 行）。
- 已 `NODE_OPTIONS="" npm run build` 重建 client/dist 成功；dist 产物确认无 600000、含 60000。
- ⚠️ 注意：旧 data.json 里若存过 >60000 的值，下次启动读取校验会失败并回落默认 10000（符合更严格限制，无需手工清理）。
- electron-main.js 改动在 `npm start`/重新打包 exe 后生效，无需 build。

## 安全重构（基于最终可运行代码，逻辑等价）
- 目标：去重 / 优化命名 / 去死代码 / 修隐藏 bug / 精简条件层级，逻辑等价于原版。
- 后端 `electron-main.js` 抽取复用：
  - `wslListEnvFilesCmd(distro, linuxPath)` + `parseEnvFileNames(stdout)`：消除 `getProjectInfo` 与 `setupWslPoller.snapshot` 重复的 `wsl.exe ls` 命令与解析。
  - `buildProjectView(project, includeAllEnvVars)`：消除 GET 列表 / GET 详情 / POST 三处重复拼装视图对象。
  - `emitEnvChanged(projectId, projectDir)`：消除 change/add/unlink 监听、WSL 轮询、切换接口 5 处重复的 `getProjectInfo + io.emit('env-changed')` 模板。
  - `closeWatcher(projectId)` + `closeAllWatchers()`：消除 setupWatcher / DELETE 接口 / before-quit 三处重复的 watcher 清理。
  - `isWslPath` 改复用 `parseWslPath`（删重复正则，真实项目路径语义等价）。
  - 修 `getActiveEnvFile` 死代码：`if (!envHash) return ''` 永不为真（md5('') 非空）→ 删除；`if (h && ...)` 冗余 `h &&` 移除。
  - 命名：`_initData` → `savedData`；`getProjectInfo` 内 WSL 列举改为 `else if` + 扁平 `for` 循环，减少嵌套层级。
- 前端 `client/src/App.jsx`：
  - `mergeProjectFields`(纯函数) + `applyProjectUpdate`(useCallback)：消除 socket 推送 / 单卡片刷新 / 切换成功 3 处重复的 `setProjects(map...)`。
  - `RefreshCwIcon` 组件：消除卡片与 header 两处重复的 refresh-cw SVG。
  - 修隐藏不一致：设置初始默认值 `5000` → `10000`，与服务端默认一致（进入后由 /settings 覆盖）。
  - 修正 ProjectCardView / getProjectInfo 关于 activeEnvFile 的注释（"失配回退"实际是"多 md5 一致时的决胜负"，不假阳性）。
- 校验：`node --check electron-main.js` 通过；`NODE_OPTIONS="" npm run build` 通过（dist 产物 index-fOkzuabQ.js）。
- 未删除 `server/index.js`（仍被 `npm run server` 脚本引用，属独立 dev 服务，非应用实际调用；列为死代码候选但保留）。
- 同步修正 MEMORY.md 中关于 activeEnvFile 的过时断言（早期记忆称"已删除兜底"，实际代码仍写且用作 md5 平局决胜负）。

## 更新 README（中/英）
- 修正 README.md / README_EN.md 中陈旧的「每 3 秒」轮询描述 → 默认「每 10 秒」+ 范围「500ms–1 分钟」（与代码一致）。
- 在「切换环境」小节补充「使用中」判定中的平局决胜负逻辑：多个 `.env.xxx` md5 相等时以最近一次切换名高亮；`.env` 与所有源文件都不一样则不高亮（诚实"未关联"）。

## 二次安全重构 + 删除无用文件 + 整理 .gitignore（基于最终可运行代码）
- 后端 electron-main.js：
  - **修复隐藏逻辑 bug**：补回 `PUT /api/projects/reorder`（复刻旧 server/index.js 逻辑，接 `{ids}` 重排 data.projects 并持久化）。此前真实应用缺此路由，前端拖拽排序调用后命中静态兜底，排序**根本不持久化**（README 却宣称"自动持久化"）。
  - 修正 getProjectInfo 中"不回退 data.json"的误导注释 → 准确描述"md5 平局时用持久化上次切换名决胜负、绝不在无匹配时兜底"。
  - 修正 setupWslPoller 两处过时注释（曾写"Linux 侧 md5sum 返回 文件名|md5"，实际是 ls 列举 + Node 侧算 md5）。
  - 删除 watcher 未读取的 `_isWsl` 属性；内联 `getAppDataPath()` 为 `REAL_USER_DATA`（仅一处使用）。
  - switch 接口响应用 `buildProjectView(project, false)` 去重（与列表/详情字段一致）。
- 前端 App.jsx：
  - 删除从未被读取的 `settingsWslInterval` 状态及其两处赋值。
  - 抽出 `pickEnvFields(d)` 复用 refreshCard / socket env-changed / switchEnv 三处相同字段对象字面量。
- 删除无用文件：整体删除 `server/` 目录（index.js 死代码 + data.json*/logs 运行时产物）；删除错放到本仓库的 `AGENTS.md`（内容写的是 EasyOps 项目）。
- package.json：移除 `server` 与 `dev` 脚本（均指向已删的 server/index.js）。
- vite.config.js：删除指向已删 server/index.js :3001 的死 proxy（/api、/socket.io），修正注释。
- .gitignore：重写为带分区注释的版本，移除已删 server/ 相关条目（server/data.json*、server/logs/），保留 node_modules/dist/release/生成图标/.env/编辑器/系统文件。
- README（中/英）：开发调试命令改为 `npm run electron-dev`；FAQ 数据路径去掉"独立服务端 server/data.json"；去掉"单独 vite dev 调试前端"的误导建议。
- 校验：`node --check electron-main.js` 通过；`NODE_OPTIONS="" npm run build` 通过（dist 产物 index-TrcRYoxR.js）。
- 同步更新 MEMORY.md：server/index.js 标记为"已删除"、新增 reorder 路由归属说明；删除 AGENTS.md 与 server/ 的痕迹。

## 三次安全重构（去重 / 去死代码 / 修隐藏 bug / 整理 .gitignore，逻辑等价）
- 前端 client/src/App.jsx：
  - **修复隐藏逻辑 bug**：删除 `<span className={`env-value env-badge ${project.appEnv || ''}`}>` 把用户可控的 `appEnv` 值注入为 CSS 类名（相关语义色块 App.css 已删除，属死代码 + 潜在无效类名）。
  - **精简渲染区**：抽出头部内联 SVG 为 `PlusIcon / UpdateIcon / SettingsIcon / InfoIcon` 组件（RefreshCwIcon 已存在），消除 return 内大段重复 inline SVG。
- 后端 electron-main.js：
  - `closeAllWatchers()` 复用 `closeWatcher(id)` 遍历清理，消除与 chokidar / WSL 轮询重复的同款 close 逻辑。
  - `getProjectInfo` 的 envFiles 构建扁平化：去掉 `let files` + `for push` 冗余，直接 `let envFiles = []` 在 WSL/本地两分支赋值（等价）。
  - 抽出 `rebuildWslWatchers(projects)` 消除 `PUT /api/settings` 内联的 WSL 重建循环（等价）。
  - 修正 createWindow 误导性注释（声称"改大到 1500×900"但代码实为 1200×720）→ 改为如实描述。
- 删除无用文件：
  - `client/src/index.css`：从未被任何文件 import（main.jsx 仅引 App.jsx→App.css），纯注释死文件。
  - `client/src/assets/`：空目录。
- .gitignore 整理：合并 client/.gitignore 到根 .gitignore（消除规则分裂），根文件重写为带分区注释版本，补齐 `dist-ssr / *.local / *.log` 等条目；删除 client/.gitignore。
- 校验：`node --check` 三个主进程脚本通过；`NODE_OPTIONS="" npm run build` 通过（dist 产物 index-o_RVa-ZP.js）。均未提交（用户未要求 commit）。
