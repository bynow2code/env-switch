# 2026-08-03

## WSL 轮询间隔限制收紧
- 需求：默认 10s（已是 10000ms，未变）；将合法范围上界从 10 分钟改为 1 分钟。
- 改动文件：
  - `electron-main.js`（真实服务端，主进程直接运行无需打包）：
    - 启动读取校验 `wslPollInterval <= 600000` → `<= 60000`（约 320 行）。
    - `PUT /api/settings` 校验与错误文案 `500–60000`（约 596/600/601 行）。
    - 注释「合法范围 [500, 60000]」。
  - `client/src/App.jsx`（前端校验 + UI）：
    - `saveSettings` 校验与错误文案 `val > 60000`（约 334/335 行）。
    - 设置弹窗 `<input max="60000">` 及 hint「Range 500–60000 ms」（约 876/882 行）。
- 已 `NODE_OPTIONS="" npm run build` 重建 client/dist 成功；dist 产物确认无 600000、含 60000。
- ⚠️ 注意：旧 data.json 里若存过 >60000 的值，下次启动读取校验会失败并回落默认 10000（符合更严格限制，无需手工清理）。
- electron-main.js 改动在 `npm start`/重新打包 exe 后生效，无需 build。

## 安全重构（基于最终可运行代码，逻辑等价）
- 目标：去重 / 优化命名 / 去死代码 / 修隐藏 bug / 精简条件层级，逻辑等价于原版。
- 后端 `electron-main.js` 抽取复用：
  - `wslListEnvFilesCmd(distro, linuxPath)` + `parseEnvFileNames(stdout)`：消除 `getProjectInfo` 与 `setupWslPoller.snapshot` 重复的 `wsl.exe ls` 命令与解析。
  - `buildProjectView(project, includeAllEnvVars)`：消除 GET 列表 / GET 详情 / POST 三处重复拼装视图对象。
  - `emitEnvChanged(projectId, projectDir)`：消除 change/add/unlink 监听、WSL 轮询、切换接口 5 处重复的 `getProjectInfo + io.emit('env-changed')` 模板。
  - `closeWatcher(projectId)` + `closeAllWatchers()`：消除 setupWatcher / DELETE 接口 / before-quit 三处重复的 watcher 清理。
  - `isWslPath` 改复用 `parseWslPath`（删重复正则，真实项目路径语义等价）。
  - 修 `getActiveEnvFile` 死代码：`if (!envHash) return ''` 永不为真（md5('') 非空）→ 删除；`if (h && ...)` 冗余 `h &&` 移除。
  - 命名：`_initData` → `savedData`；`getProjectInfo` 内 WSL 列举改为 `else if` + 扁平 `for` 循环，减少嵌套层级。
- 前端 `client/src/App.jsx`：
  - `mergeProjectFields`(纯函数) + `applyProjectUpdate`(useCallback)：消除 socket 推送 / 单卡片刷新 / 切换成功 3 处重复的 `setProjects(map...)`。
  - `RefreshCwIcon` 组件：消除卡片与 header 两处重复的 refresh-cw SVG。
  - 修隐藏不一致：设置初始默认值 `5000` → `10000`，与服务端默认一致（进入后由 /settings 覆盖）。
  - 修正 ProjectCardView / getProjectInfo 关于 activeEnvFile 的注释（"失配回退"实际是"多 md5 一致时的决胜负"，不假阳性）。
- 校验：`node --check electron-main.js` 通过；`NODE_OPTIONS="" npm run build` 通过（dist 产物 index-fOkzuabQ.js）。
- 未删除 `server/index.js`（仍被 `npm run server` 脚本引用，属独立 dev 服务，非应用实际调用；列为死代码候选但保留）。
- 同步修正 MEMORY.md 中关于 activeEnvFile 的过时断言（早期记忆称"已删除兜底"，实际代码仍写且用作 md5 平局决胜负）。
