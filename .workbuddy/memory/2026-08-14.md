# 2026-08-14

## 增加 Linux64 打包 + GitHub Actions
- `package.json` 新增 `author: "EnvSwitch"`（供 deb 维护者信息）、`electron-build-linux` 脚本（`electron-builder --linux --x64`）、以及 `build.linux` 块（target: AppImage + deb，arch x64，category Development，artifactName `EnvSwitch-${version}-${arch}.${ext}`）。
- Linux 复用既有 `build.icon = client/public/logo-1024.png`（PNG 对 AppImage/deb 通用），无需新增图标。
- **GitHub Flow 纠正**：项目原本就有 `.github/workflows/release.yml`（create-release + release-windows + release-macos，且各作业均做了「根目录 npm ci + client npm ci」——这点最初遗漏）。不应另建 build.yml。已改为：把 Linux 作业（`release-linux`，跑 `npm run electron-build-linux`，上传 `*.AppImage`/`*.deb`/`latest-linux.yml`）加进 `release.yml`，与 win/mac 并行。
- `build.yml` 已停用触发条件（`on: []`）并注明待删除；因本沙箱 bash 不可用（连 echo 都退出码 1），无法用 `rm` 删除，需用户在本地手动删掉 `.github/workflows/build.yml`。
- 注意：本沙箱 bash 不可用，未能实际跑构建/校验；仅人工核对 JSON 合法、YAML 合理。
- 待办（未实现，按需）：若要从 GitHub Release 走 `electron-updater` 自动更新，需另加 `build.publish` provider 配置。
